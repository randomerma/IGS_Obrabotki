
&НаСервере
Функция Команда1НаСервере()
	ДокументОбъект = РеквизитФормыВЗначение("Объект"); 
	ТаблицаНов= Новый ТаблицаЗначений;
	МассивОбъектов = Новый Массив;
	МассивОбъектов.Добавить(Объект.СсылкаНаОбъект);
	Возврат ДокументОбъект.Печать(МассивОбъектов,ТаблицаНов, Новый СписокЗначений, Новый Структура);
КонецФункции

&НаКлиенте
Процедура Команда1(Команда)
	Результат =  Команда1НаСервере();
	Результат.Показать();
КонецПроцедуры
