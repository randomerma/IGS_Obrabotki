
&НаСервере
Функция ТестНаСервере()
    ТекущОБ = РеквизитФормыВЗначение("Объект");
    МассивОбъектов = новый Массив;
	МассивОбъектов.Добавить(Реализация);
	ТЗ = Новый ТаблицаЗначений;
	Тз.Колонки.Добавить("ИмяВРЕГ");
	СписокОбъектов = Новый СписокЗначений;
	Возврат	ТекущОБ.Печать(МассивОбъектов, ТЗ,СписокОбъектов , Неопределено);
КонецФункции

&НаКлиенте
Процедура Тест(Команда)
	Результат = ТестНаСервере(); 
	Результат.Показать();
КонецПроцедуры
