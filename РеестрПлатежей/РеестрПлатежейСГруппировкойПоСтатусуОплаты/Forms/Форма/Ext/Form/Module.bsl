&НаСервере
Функция реестрПриИзмененииНаСервере()
    ТекущОБ = РеквизитФормыВЗначение("Объект");
    МассивОбъектов = новый Массив;
	МассивОбъектов.Добавить(реестр);
	
	
Возврат	ТекущОБ.Печать(МассивОбъектов, Неопределено, Неопределено, Неопределено);
КонецФункции


&НаКлиенте
Процедура реестрПриИзменении(Элемент)
	ИтоговыйДокумент = реестрПриИзмененииНаСервере();
	ИтоговыйДокумент.Показать();
КонецПроцедуры

